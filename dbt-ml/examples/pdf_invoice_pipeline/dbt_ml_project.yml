name: pdf_invoice_pipeline
version: "0.1.0"
profile: pdf_invoice_pipeline

extraction:
  default_backend: pdf

source-paths: ["sources"]
model-paths: ["models"]
transform-paths: ["transforms"]
target-path: "target"
