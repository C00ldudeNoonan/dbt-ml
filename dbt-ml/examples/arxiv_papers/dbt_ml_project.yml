name: arxiv_papers
version: "0.1.0"
profile: arxiv_papers

extraction:
  default_backend: json

source-paths: ["sources"]
model-paths: ["models"]
transform-paths: ["transforms"]
target-path: "target"
